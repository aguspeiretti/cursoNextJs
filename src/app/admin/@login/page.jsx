import { LoginForm } from "@/components/auth/LoginForm";
import React from "react";

const page = () => {
  return (
    <div className="w-full h-full">
      <LoginForm />
    </div>
  );
};

export default page;
